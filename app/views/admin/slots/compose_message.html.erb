
<%= render partial: "compose_message",
           locals: {
             model: @ComposeMessage,
             form_url: send_message_admin_slot_path(@ProjectSlot),
             message:
               "This message will be sent to #{@ProjectSlot.volunteers.count} " +
               "#{'volunteer'.pluralize @ProjectSlot.volunteers.count} registered to serve on the " +
               "#{@ProjectSlot.project.project_name} project on " +
               "#{@ProjectSlot.slot_date.strftime('%d/%m/%Y')} " +
               "at #{@ProjectSlot.start_time} (#{@ProjectSlot.humanized_slot_type})",
             factor: @ProjectSlot.volunteers.count,
             back_url: admin_slot_path(@ProjectSlot),
             back_message: 'Back to slot details'
           } %>
